{
	"name": "site",
	"private": true,
	"scripts": {
		"dist": "bun ./scripts/dist.ts",
		"dev": "nodemon --watch . --ext ts,js,json,html,css --ignore dist/ --exec 'bun run dist'",
		"preview": "bunx http-server dist"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"nodemon": "^3.1.10"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}