---
// AUTO-GENERATED BY GENERATE-DOCS.TS
// DO NOT EDIT
import Header from "@/components/Header.astro";
import Section from "@/components/Section.astro";
import Subsection from "@/components/Subsection.astro";
import SideLayout from "../../layouts/SideLayout.astro";
---

<SideLayout title="Dialog - PaleUI">
	<Header
		title="Dialog"
		subtitle="Modal that overlays and renders separate content."
		links={[
			{
				title: "MDN: <dialog>",
				url: "https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/dialog",
			},
		]}
	>
		<button onclick="share_modal.showModal()">Share</button>
		<dialog id="share_modal">
			<hgroup>
				<h3>Share link</h3>
				<p>Anyone who has this link will be able to view this.</p>
			</hgroup>
			<input type="text" value="https://paleui.ahme.dev" style="width: 100%;" />
			<div style="margin: 0.5rem 0;"></div>
			<div role="group">
				<button style="background-color: indianred;">X</button>
				<button style="background-color: indianred;">LinkedIn</button>
			</div>
			<form method="dialog">
				<button class="secondary">Close</button>
			</form>
		</dialog><Fragment slot="below"
			><p>
				The modern semantic approach used here has <a
					target="_blank"
					rel="noopener noreferrer"
					href="https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/dialog#browser_compatibility"
					>wide support</a
				> in browsers. However, Javascript is required to toggle the modal (less than
				one-line), and because of this a different approach will be provided in future
				releases.
			</p></Fragment
		>
	</Header>
	<Section title="Structure"
		><Subsection
			><Fragment slot="above"
				><p>
					The dialog is built using the native <code>&lt;dialog&gt;</code> element,
					which is natively supported by browsers to trigger a modal without special
					styling. Open it by using an id and <code>showModal()</code>.
				</p><p>
					<code>.close</code> — Close button positioned at the top right (optional).
				</p><p>
					<code>hgroup</code> — Header group containing title and description (optional).
				</p><p>
					<code>p</code> — Description text within the header group (optional).
				</p><p>
					<code>h1, h2, h3, h4, h5, h6</code> — Dialog heading (optional).
				</p><p>
					<code>div:last-child, form:last-child</code> — Footer area with action buttons
					(optional).
				</p></Fragment
			><button onclick="mdl.showModal()">
				<!-- trigger text -->
			</button>

			<dialog id="mdl">
				<hgroup>
					<!-- title and description -->
				</hgroup>

				<!-- dialog content -->

				<div>
					<!-- buttons or footer (auto aligned to the right) -->
				</div>
			</dialog></Subsection
		></Section
	><Section title="Examples"
		><Subsection title="Usage"
			><Fragment slot="above"
				><p>
					Open the dialog using <code>showModal()</code> on the element. Close it
					with a <code>&lt;form method="dialog"&gt;</code> containing a submit button.
				</p></Fragment
			><button onclick="share_modal.showModal()">Share</button>
			<dialog id="share_modal">
				<hgroup>
					<h3>Share link</h3>
					<p>Anyone who has this link will be able to view this.</p>
				</hgroup>
				<input
					type="text"
					value="https://paleui.ahme.dev"
					style="width: 100%;"
				/>
				<div style="margin: 0.5rem 0;"></div>
				<div role="group">
					<button style="background-color: indianred;">X</button>
					<button style="background-color: indianred;">LinkedIn</button>
				</div>
				<form method="dialog">
					<button class="secondary">Close</button>
				</form>
			</dialog></Subsection
		></Section
	>
</SideLayout>
