/**
 * 🔲 Modern CSS-only UI library built on semantic HTML, shadcn-compatible tokens, and visual regression tests.
 *
 * Import the CSS files from the package's `lib/` directory.
 *
 * @module
 */

export const packageName = "@ahme-dev/paleui";
